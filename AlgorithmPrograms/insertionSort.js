const util = require('../Utility/Utility.js');

var arr = new Array();
arr = util.oneDarray();

/**  convertion string to number */
// for (let i=0;i<arr.length; i++)
// {
//     arr[i]=parseInt(arr[i]);
// }

var a=util.InsertionSort(arr);
console.log(a);
`       `