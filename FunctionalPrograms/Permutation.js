/*
    * @purpose: json parsing
    * @author: Shweta Ingle
    * @ version: 1.0
    * since: 21-08-2019
*/

const util = require('../Utility/Utility.js');

var str = new String();

console.log("Enter the string: ");
str = util.readInput();

permutation(str);
